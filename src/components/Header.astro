---
const nav = [
  { href: "about", label: "About" },
  { href: "projects", label: "Projects" },
  { href: "timeline", label: "Career Timeline" },
  { href: "tech", label: "Tech" },
  { href: "services", label: "Services" },
  { href: "contact", label: "Contact" },
]
---

<header class="w-full bg-brand-surface border-b border-neutralTech-200">
  <div class="max-w-container mx-auto px-4 py-4 flex items-center justify-between">

    <!-- Logo / Name -->
    <a href="/" class="text-h4 font-semibold text-brand-wine hover:text-brand-wineMuted transition-colors fast ease-linearish">
      Luiza Gusmão
    </a>

    <!-- Navigation -->
    <nav>
      <ul class="flex gap-6 text-body">
        {nav.map(item => (
          <li>
            <a 
              href={item.href}
              class="text-neutralTech-700 hover:text-brand-wineMuted transition-colors fast ease-linearish"
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

  </div>
</header>
